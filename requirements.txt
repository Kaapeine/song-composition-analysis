# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Storage
boto3>=1.34.0

# Analysis
numpy>=1.26.0
scipy>=1.13.0
soundfile>=0.12.3
librosa>=0.10.0
pyloudnorm>=0.1.1
music21>=9.1.0

# These must be installed separately in order (handled by Dockerfile.base):
# 1. pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# 2. pip install ninja
# 3. pip install natten --find-links https://shi-labs.com/natten/wheels/cpu/torch230/index.html
# 4. pip install git+https://github.com/CPJKU/madmom
# 5. pip install allin1
